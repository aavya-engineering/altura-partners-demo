/**
 * EmailJS credentials — edit these values directly.
 *
 * In the EmailJS dashboard (template_alok6si → Settings), use:
 *   From Name:  {{name}}
 *   From Email: leave "Use Default Email Address" checked (your connected Gmail)
 *   Reply To:   {{email}}
 *   To Email:   info@alturapartners.ai  (or your inbox)
 *
 * In the template body, include the sender's email, e.g.:
 *   From: {{name}}
 *   Email: {{email}}
 *   Inquiry: {{inquiry_type}}
 *   Message: {{message}}
 *
 * Note: Gmail always shows your connected service email next to the name in the
 * inbox header. Reply-To is set to the form user's email so Reply goes to them.
 */
window.ALTURA_EMAILJS = {
  publicKey: 'j4f5CdWZc15B50UN4',
  serviceId: 'service_3p3vtd6',
  templateId: 'template_xatws5b',
};
